{
  "description": "frontend-designer-pro: notify the user when a newer plugin version is published",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/check-update.js\"",
            "timeout": 8
          }
        ]
      }
    ]
  }
}
